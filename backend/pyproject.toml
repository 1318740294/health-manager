[project]
name = "health-manager-backend"
version = "0.1.0"
description = "AI 健康管理对话助手后端"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.110.0",
    "uvicorn[standard]>=0.27.0",
    "openai>=1.12.0",
    "python-dotenv>=1.0.0",
]

[tool.uv]
dev-dependencies = []
